extends ../base
include ../mixins/video

block content
    div.profile_data
        if isProduction
            img(src=user.avatarUrl).avatar
        else
            img(src="/" + user.avatarUrl).avatar
        h3=user.name
    div.video-grid
        each video in user.videos 
            +video(video)
        else
            span.empty__massage 업로드한 영상이 없습니다