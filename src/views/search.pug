extends base
include mixins/video

block content 
    form(method="get").search__form
        input(type="text" placeholder="제목으로 검색하기" name="keyword")
        button 
            i.fas.fa-search
   
    div.video-grid
        each video in videos
            +video(video)
        else
            span.empty__massage 비디오가 없습니다