extends base
include mixins/video

block content 
    form(method="get").search__form
        input(type="text" placeholder="Search by title" name="keyword")
        input(type="submit", value="Search now")
   
    div.video-grid
        each video in videos
            +video(video)
        else
            span.empty__massage No Videos Here